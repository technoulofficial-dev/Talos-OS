/**
 * Cortex — User Context & Memory System
 *
 * Manages the infinite memory system:
 *   - Thread of Fate (rolling conversation memory)
 *   - Identity Core (user's long-term goals/personality)
 *   - Context Budget (95% snapshot auto-trigger)
 */

export * from "./context-budget.js";